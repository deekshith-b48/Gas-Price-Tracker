/** @type {import('next').NextConfig} */
const nextConfig = {
  // Optional: Add any specific Next.js configurations here
  // For example, to enable React Strict Mode (default for create-next-app)
  reactStrictMode: true,
  // If you need to enable experimental features, e.g.,
  // experimental: {
  //   serverActions: true,
  // },
};

export default nextConfig;